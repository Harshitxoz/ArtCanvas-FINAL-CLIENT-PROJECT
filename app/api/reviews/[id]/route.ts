import {NextResponse} from "next/server";import {ObjectId} from "mongodb";import {getDb} from "@/lib/db";import {requireAdmin} from "@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;if(!ObjectId.isValid(id))return NextResponse.json({error:"Invalid id"},{status:400});const b=await req.json();await getDb().then(db=>db.collection("reviews").updateOne({_id:new ObjectId(id)},{$set:{approved:Boolean(b.approved),updatedAt:new Date()}}));return NextResponse.json({ok:true})}catch(e){const u=e instanceof Error&&e.message==="UNAUTHORIZED";return NextResponse.json({error:u?"Unauthorized":"Could not update review"},{status:u?401:500})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;if(!ObjectId.isValid(id))return NextResponse.json({error:"Invalid id"},{status:400});await getDb().then(db=>db.collection("reviews").deleteOne({_id:new ObjectId(id)}));return NextResponse.json({ok:true})}catch(e){const u=e instanceof Error&&e.message==="UNAUTHORIZED";return NextResponse.json({error:u?"Unauthorized":"Could not delete review"},{status:u?401:500})}}
