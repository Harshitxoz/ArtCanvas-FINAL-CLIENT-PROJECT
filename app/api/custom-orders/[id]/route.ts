import {NextResponse} from "next/server";import {ObjectId} from "mongodb";import {getDb} from "@/lib/db";import {requireAdmin} from "@/lib/auth";import {customOrderStatusSchema} from "@/lib/validations";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;if(!ObjectId.isValid(id))return NextResponse.json({error:"Invalid id"},{status:400});const p=customOrderStatusSchema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid status"},{status:400});await getDb().then(db=>db.collection("customOrders").updateOne({_id:new ObjectId(id)},{$set:{...p.data,updatedAt:new Date()}}));return NextResponse.json({ok:true})}catch(e){const u=e instanceof Error&&e.message==="UNAUTHORIZED";return NextResponse.json({error:u?"Unauthorized":"Could not update request"},{status:u?401:500})}}
