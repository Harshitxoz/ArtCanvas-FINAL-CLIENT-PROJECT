import {NextResponse} from "next/server";import {getDb} from "@/lib/db";import {getSession,requireAdmin} from "@/lib/auth";import {customOrderRequestSchema} from "@/lib/validations";
const schema = customOrderRequestSchema;
export async function GET(){try{await requireAdmin();const docs=await (await getDb()).collection("customOrders").find({}).sort({createdAt:-1}).limit(500).toArray();return NextResponse.json(docs.map(d=>({...d,_id:String(d._id)})))}catch(e){const u=e instanceof Error&&e.message==="UNAUTHORIZED";return NextResponse.json({error:u?"Unauthorized":"Database unavailable"},{status:u?401:500})}}
export async function POST(req:Request){try{const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid request",details:p.error.flatten().fieldErrors},{status:400});const user=await getSession();const r=await (await getDb()).collection("customOrders").insertOne({...p.data,userId:user?.id||null,status:"new",adminNotes:"",createdAt:new Date(),updatedAt:new Date()});return NextResponse.json({ok:true,id:String(r.insertedId)},{status:201})}catch{return NextResponse.json({error:"Could not submit custom request"},{status:500})}}
