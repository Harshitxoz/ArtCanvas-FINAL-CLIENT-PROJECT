import {NextResponse} from "next/server";import {ObjectId} from "mongodb";import {getDb} from "@/lib/db";import {requireAdmin} from "@/lib/auth";import {z} from "zod";
const schema=z.object({name:z.string().min(2).max(80),slug:z.string().min(2).max(100).regex(/^[a-z0-9-]+$/),description:z.string().max(500),image:z.string().max(1000),active:z.boolean()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;if(!ObjectId.isValid(id))return NextResponse.json({error:"Invalid id"},{status:400});const p=schema.partial().safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid category"},{status:400});const db=await getDb();await db.collection("categories").updateOne({_id:new ObjectId(id)},{$set:{...p.data,updatedAt:new Date()}});return NextResponse.json({ok:true})}catch(e){const u=e instanceof Error&&e.message==="UNAUTHORIZED";return NextResponse.json({error:u?"Unauthorized":"Could not update category"},{status:u?401:500})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;if(!ObjectId.isValid(id))return NextResponse.json({error:"Invalid id"},{status:400});const db=await getDb();const cat=await db.collection("categories").findOne({_id:new ObjectId(id)});if(!cat)return NextResponse.json({error:"Category not found"},{status:404});const count=await db.collection("products").countDocuments({category:String(cat.slug)});if(count>0)return NextResponse.json({error:"This category contains products. Move or update those products before deleting.",hasProducts:true,productCount:count},{status:409});await db.collection("categories").deleteOne({_id:new ObjectId(id)});return NextResponse.json({ok:true})}catch(e){const u=e instanceof Error&&e.message==="UNAUTHORIZED";return NextResponse.json({error:u?"Unauthorized":"Could not delete category"},{status:u?401:500})}}
